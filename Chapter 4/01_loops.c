// loops are used when we want a repetation of a block of code multiple times or run a piece of code "x" numbers of time without human labour.
// every loop works in three phases
// Initialization: Setting up the starting point (e.g., int i = 0).

// Condition: The test expression checked before or after each cycle (e.g., i < 10).

// Loop Body: The statements executed on each iteration.

// Update Expression: Changing the control variable toward the termination state (e.g., i++).

#include <stdio.h>

int main(){
    
    return 0;
}