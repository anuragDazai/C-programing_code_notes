#include <stdio.h>

int main(){
    int a = 6;
    int b = 4;
    // we are getting the remainder of a divided by b using modulus operator and printing it to the console.
    printf("The modulus operator gives us the remainder from a division operation, so the value of a %% b is %d", a % b);
    return 0;
}