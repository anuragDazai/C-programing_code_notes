#include <stdio.h>

int main(){
    // Write a program to check whether a number is divisible by 97 or not
    int a = 22537271;
    printf("The number is divisible by 97 only if a%%97 is 0: %d", a%97);
    return 0;
}